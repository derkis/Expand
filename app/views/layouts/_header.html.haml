%header
  = image_tag("logo.png", :alt => "Expand Logo")
  %nav
    %ul
      - if signed_in?
        %li
          Welcome,  
          - user = current_user
          = link_to "#{user.name}", user_path(user.id) 
        %li= link_to "Logout", :logout
      - else  
        %li= link_to "Sign in", :login
      %li= link_to "Home", '/'
      %li= link_to "Help", '#'  